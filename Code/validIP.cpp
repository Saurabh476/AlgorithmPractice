#include<bits/stdc++.h>
using namespace std;

void validIp(vector<string> &v, string s, int index,int oct){
	if(index >= s.size())
		return;
	if(s[index]>=48 && s[index] <=57){
		

		
	}
	// if(s[index]<=)
}

int main()
{
	string s = "";
	std::vector<string> v;
	char c= '9';
	int a = c;
	validIp(v,s,2,3);
	cout<<a;
}
